<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
    <body>
      <header>
        <nav class="be-red white large-padding">
          <ul class="no-list row center-xs middle-xs">
            <ul class="col-md">
            <%= link_to "Home", root_path %> 
            </ul>
            <ul class="col-md">
            <%= link_to "Bio", pages_bio_path %>
            </ul>
            <ul class="col-md">
            <%= link_to "Servicios", pages_service_path %>
            </ul>
            <ul class="col-md">
            <%= link_to "Works", pages_work_path %>
            </ul>
          </ul>
        </nav>
      </header>
  
    <%= yield %>
    


    </body>
</html>
